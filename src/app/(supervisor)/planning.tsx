import { Text, View } from "react-native";

export default function SupervisorPlanning() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Supervisor - Planning</Text>
    </View>
  );
}