import { Text, View } from "react-native";

export default function SupervisorOrders() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Supervisor - Orders</Text>
    </View>
  );
}